// export const validateAcquaintance = (rule, value, callback) => {
//     // 输入 8--，value 为 8
//     // 估计这里内部使用了 parseInt() / parseFloat()
//     if (value < 0 || value > 100) {
//         callback(new Error('熟悉程度在 0 至 100 %之间'))
//     } else {
//         callback()
//     }
// }